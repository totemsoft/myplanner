<?xml version="1.0" encoding="UTF-8"?>
<web-app version="2.4" xmlns="http://java.sun.com/xml/ns/j2ee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd">

    <display-name>MyPlanner J2EE 1.4 GWT Webapp</display-name>

    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>
            classpath:applicationContext.xml
        </param-value>
    </context-param>

    <listener>
        <listener-class>com.argus.financials.myplanner.web.servlet.SetupContextListener</listener-class>
    </listener>

    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <welcome-file-list>
        <welcome-file>Login.html</welcome-file>
    </welcome-file-list>

    <resource-env-ref>
        <description>MyPlanner DataSource</description>
        <resource-env-ref-name>jdbc/DataSource_myplanner</resource-env-ref-name>
        <resource-env-ref-type>javax.sql.DataSource</resource-env-ref-type>
    </resource-env-ref>

    <!--
      - Servlet that dispatches request to registered handlers (Controller implementations).
      - Has its own application context, by default defined in "/WEB-INF/{servlet-name}-servlet.xml",
      - A web app can contain any number of such servlets.
      - Note that this web app has a shared root application context, serving as parent
      - of all DispatcherServlet contexts.
      -->
	<servlet>
		<servlet-name>gwt-controller</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<load-on-startup>0</load-on-startup>
	</servlet>
    <servlet>
    	<servlet-name>SecurityController</servlet-name>
    	<servlet-class>com.argus.financials.myplanner.gwt.security.server.SecurityController</servlet-class>
    </servlet>

	<servlet-mapping>
	    <servlet-name>gwt-controller</servlet-name>
	    <url-pattern>*.do</url-pattern>
	</servlet-mapping>
    <servlet-mapping>
    	<servlet-name>SecurityController</servlet-name>
    	<url-pattern>/com.argus.financials.myplanner.gwt.security.Login/SecurityService</url-pattern>
    </servlet-mapping>

</web-app>